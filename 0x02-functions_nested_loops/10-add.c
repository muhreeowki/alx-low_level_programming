#include <stdio.h>
#include <math.h>

/* Function prototype */
int add(int, int);

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	return (0);
}


/**
 * add - Adds two integers together and returns the sum.
 *
 * @y: Integer to be added
 * @x: Integer to be added
 * Return: Sum of two x and y
 */
int add(int y, int x)
{
	return (y + x);
}
